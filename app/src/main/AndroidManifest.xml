<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="parentapp.oac.com.greendaoforandroidstudio" >

    <application
        android:name="example.isuru.com.greendaoforandroidstudio.BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="example.isuru.com.greendaoforandroidstudio.ui.activity.MessageListActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="example.isuru.com.greendaoforandroidstudio.ui.activity.EditMessageActivity"
            android:configChanges="orientation|screenSize"/>
    </application>


</manifest>
